<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post</title>
    <link rel="stylesheet" href="../public/css/styles.css">
</head>
<body>
        <h1><%= post.author.name %></h1>
        <h3><%= post.createdAt %></h3>
        <h2><%= post.title %></h2>
        <h4><%= post.content %></h4>
        <% post.comments.forEach(com => { %>
            <p><b> <%= com.author.name %> </b></p>
            <p> <%= com.createdAt %> </p>
            <p> <%= com.comment %> </p>
        <% }); %>
</body>
</html>